import SectionTitle from "../SectionTitle";

export default function Categories() {
    return (
        <section>
            <SectionTitle subtitle="Discover by topic">Categories</SectionTitle>
        </section>
    );
}
